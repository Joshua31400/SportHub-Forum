<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SPORT-HUB / POST</title>
    <link rel="stylesheet" href="/static/principal.css">
    <link rel="stylesheet" href="/static/post.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
<div class="container">
    <header class="main-header">
        <h1>SPORT-HUB</h1>
        <nav>
            <a href="/" class="active">Forum</a>
            <a href="/profile">Profil</a>
            {{if .IsAuthenticated}}
                <a href="/logout">Déconnexion</a>
            {{else}}
                <a href="/login">Connexion</a>
                <a href="/register">Inscription</a>
            {{end}}
        </nav>
    </header>

    <main class="post-detail">
        <article class="post">
            <div class="post-header">
                <h2>{{.Post.Title}}</h2>
                <div class="post-meta">
                    <span class="author">
                        <i class="fas fa-user"></i> Posté par: {{.Post.Username}}
                    </span>
                    <span class="category">
                        <i class="fas fa-tag"></i> Catégorie: {{.Post.CategoryName}}
                    </span>
                    <span class="date">
                        <i class="far fa-clock"></i> {{.Post.CreatedAt.Format "02/01/2006 à 15:04"}}
                    </span>
                    <span class="like-action">
                        <a href="/like-post?id={{.Post.ID}}" class="like-button {{if .IsLiked}}liked{{end}}">
                            <i class="{{if .IsLiked}}fas{{else}}far{{end}} fa-heart"></i>
                            <span class="likes-count">{{.LikeCount}}</span>
                        </a>
                    </span>
                </div>
            </div>
            <div class="post-content">
                {{.Post.Content}}
            </div>
            <section class="comments-section">
                <h3>Commentaires ({{len .Comments}})</h3>
                <div class="comments-list">
                    {{if .Comments}}
                        {{range .Comments}}
                            <div class="comment">
                                <div class="comment-header">
                                    <span class="comment-author"><i class="fas fa-user"></i> {{.Username}}</span>
                                    <span class="comment-date"><i
                                                class="far fa-clock"></i> {{.CreatedAt.Format "02/01/2006 à 15:04"}}</span>
                                </div>
                                <div class="comment-content">
                                    {{.Content}}
                                </div>
                            </div>
                        {{end}}
                    {{else}}
                        <p class="no-comments">Aucun commentaire pour l'instant. Soyez le premier à commenter !</p>
                    {{end}}
                </div>

                <div class="comment-form">
                    <h4>Ajouter un commentaire</h4>
                    <form action="/addcomment" method="POST">
                        <input type="hidden" name="post_id" value="{{.Post.ID}}">
                        <textarea name="content" placeholder="Votre commentaire..." required></textarea>
                        <button type="submit" class="submit-comment">
                            <i class="fas fa-paper-plane"></i> Publier
                        </button>
                    </form>
                </div>
            </section>
            <div class="post-actions">
                <a href="/" class="back-button">
                    <i class="fas fa-arrow-left"></i> Retour au forum
                </a>
            </div>
        </article>
    </main>

    <footer>
        <p>&copy; 2025 SPORT-HUB - Forum communautaire sportif</p>
    </footer>
</div>
</body>
</html>